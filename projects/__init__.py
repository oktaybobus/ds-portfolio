"""Portfolio projects.

Each subpackage is one project distilled from a course notebook. A project owns
its ``config.yaml`` and a ``pipeline.py`` implementing
:class:`dsjourney.pipeline.ProjectPipeline`; all shared machinery lives in the
``dsjourney`` package.
"""
