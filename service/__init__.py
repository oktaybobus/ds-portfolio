"""FastAPI service exposing every trained project in the portfolio.

This is infrastructure rather than a portfolio project, so it lives outside
``projects/``: it has no dataset, no model of its own, and nothing to train.
"""
